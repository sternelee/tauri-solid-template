// ============================================================================
// Raycast LaunchType Constants Implementation
// ============================================================================
// Based on Raycast API specification for launch types

export enum LaunchType {
  UserInitiated = "user-initiated",
  Background = "background",
  OnStartup = "on-startup",
}

